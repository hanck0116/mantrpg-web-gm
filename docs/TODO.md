# TODO

## 원본 자료

- 원본 zip 업로드 필요: `source/ManRPG_v18_FINAL_병합패키지.zip`
- 원본 `deep-research-report.md` 제공 시 `docs/deep-research-report.md` 자리표시자 교체
- zip 업로드 후 압축 해제 및 내부 파일 구조 확인
- zip 내부에서 통합본, 시트지, 플레이어 파일, 규칙 파일 식별
- 식별한 원본 규칙으로 `docs/rules-extracted.md` 갱신

## 로컬 규칙 엔진

- 현재 파생 수치, 주사위, 기본 공격 공식은 원본 zip 미확인 상태의 임시 구현이다.
- 원본 통합본과 충돌하는 공식은 통합본 기준으로 교체한다.
- 스킬, 마법, 아이템, 방어, 이동 세부 규칙을 로컬 TypeScript 코드로 확정한다.
- 보상 테이블과 심상세계 절차를 AI 없이 코드로 구현한다.

## UI / PWA

- 7x7 고정 맵에 공격 예고, 파괴된 지형, 불, 낙인, 마법 효과, 환경 변화를 표시한다.
- 저장/불러오기 stub을 실제 상태 검증 로직으로 확장한다.
- 모바일 화면에서 긴 전투 로그와 행동 버튼 사용성을 점검한다.
- 서비스 워커 캐싱 전략을 다음 단계에서 확정한다.

## 선택형 AI

- `callLLM`은 현재 실제 API 호출 없이 fallback 응답만 반환한다.
- Groq/Gemini/OpenRouter provider 파일은 타입과 stub만 존재한다.
- AI는 GM 묘사, 애매한 행동 해석, 희소한 스킬/마법 설명 생성에만 사용한다.
- AI가 판정, 주사위, 보상, 적 행동을 결정하지 못하도록 유지한다.
